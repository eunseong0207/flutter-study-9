/// main 함수가 프로그램의 시작점
void main() {
  // 1. 1부터 100까지 더하기
  // 1-1. 1부터 100까지 반복이 잘 되는지 print 함수로 출력해보기

  int a = 0;
  for (int i = 1; i <= 100; i++) {
    a += i;
  }
  print(a);

  // 2. 별 (*) 피라미드 찍기
  // 3. 화문 판별
}
